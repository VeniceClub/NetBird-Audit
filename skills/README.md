# ChatGPT Skill

`netbird-audit-development/` is the reusable development Skill for this repository. It contains the project invariants, debugging order, architecture references and release workflow for future ChatGPT-assisted development.

A separately validated `skill.zip` can be uploaded to ChatGPT's Skills interface. Keep the source Skill directory in Git so changes are versioned with the codebase.
